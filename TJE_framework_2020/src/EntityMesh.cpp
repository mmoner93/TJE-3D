#include "EntityMesh.h"


void EntityMesh::render(Light* light) {
	//no aplica , se usa para guardar la primera carga
}


void EntityMesh::update(float seconds_elapsed) {
	//no aplica , se usa para guardar la primera carga
}